# Loaded by `vite build --mode dev` for the DEV site build.
# Same-origin: SPA + API share https://dev.erp.vigneshgrowthlab.com.
# Public value (baked into the bundle) — safe to commit.
VITE_API_URL=https://dev.erp.vigneshgrowthlab.com
