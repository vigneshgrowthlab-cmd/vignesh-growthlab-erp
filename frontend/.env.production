# Vite reads this automatically during `npm run build` (production mode).
# Same-origin deploy: SPA and API share the same host (Railway or custom domain),
# so all /api/v1/... and /uploads/... calls hit the current origin via relative path (no CORS).
VITE_API_URL=
